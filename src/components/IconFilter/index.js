import styled from 'styled-components';
import iconFilter from '../../assets/img/icon-filter.svg';

const IconFilter = styled.img.attrs({
  src: iconFilter,
  alt: 'Ícone que simboliza uma lista de itens',
})`
  margin: auto 17px;
`;

export default IconFilter;
