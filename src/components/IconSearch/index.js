import styled from 'styled-components';
import iconSearch from '../../assets/img/icon-search.svg';

const IconSearch = styled.img.attrs({ src: iconSearch, alt: 'ícone de busca' })`
  width: 200px;
`;

export default IconSearch;
