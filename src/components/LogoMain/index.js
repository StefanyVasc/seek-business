import styled from 'styled-components';
import logo from '../../assets/img/logo.svg';

const LogoMain = styled.img.attrs({ src: logo, alt: 'logo da collabcode' })`
  width: 400px;
`;

export default LogoMain;
