import styled from 'styled-components';

const ServiceGuide = styled.section`
  display: flex;
  justify-content: start;
  align-items: center;
  background: rgba(58, 64, 66, 0.4);
  height: 40px;
`;

export default ServiceGuide;
